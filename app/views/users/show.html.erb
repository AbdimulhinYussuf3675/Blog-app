<div class="users-container">
    <div class="user-box">
        <img class="user-img" src="<%= @user.photo %>" alt="user"/>
        <div class="user-info-box">
            <h2 class="user-name"><%= @user.name %></h2>
            <p class="user-post-count">Number of posts: <%= @user.posts_counter%></p>
        </div>
    </div>

    <div class="user-bio">
        <p>Bio</p> <br/>
        <p><%= @user.bio %></p>
    </div>


    <% @user.recent_posts.each do |post| %>
        <%= link_to user_post_path(@user.id, post.id), class:"text" do %>
        <div class="user-recent-post">
            <h2 class="user-post-title">Post : <%= post.title %></h2>
            <p class="user-post-text"><%= post.text %></p>
            <h3 class="user-post-counters">Comments: <%= post.comments_counter%>, Likes: <%= post.likes_counter %><h3>
            </div>
            <% end %>
            <div class="like_comment">
                <%= form_with url: user_post_likes_path(current_user, post) do |form| %>
                    <%= form.submit "Like", class: "btn" %>
                <% end %>
                <%= link_to new_user_post_comment_path(current_user, post), class:"text-dec" do %>
                    <button class="btn_comment">Comment</button>
                <% end %>
            </div>
    <% end%>

    <button class="button">
        <%= link_to "See all posts", user_posts_path(@user), class:"text-dec" %>
    </button>
</div>
